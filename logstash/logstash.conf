input { 
  beats {
    port => 5044
  }
}

filter {
  date {
    match => [ "timestamp" , "yyyy-MM-ddTHH:mm:ssZ" ]
  }
}

output {
  elasticsearch { hosts => [ "es01:9200", "es02:9200" ] }
  stdout { codec => rubydebug }
}
